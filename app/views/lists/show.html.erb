<p><strong>Movie</strong></p>
<ul class="list-group">
  <% @list.movies.each do |movie| %>
    <li class="list-group-item"> <%= movie.title %>  </li>
  <% end %>
</ul>
<%= link_to("ajouter film",  movies_new_path) %>
<br>
